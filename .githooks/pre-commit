#!/bin/sh

set -e

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT/src-tauri"

echo "Formatting..."
cargo fmt --all

echo "Adding formatting changes..."
git add -u

echo "Running clippy..."
cargo clippy --all-targets --all-features -- -D warnings

echo "✓ Pre-commit checks passed"
